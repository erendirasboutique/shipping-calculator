# Erendira's Boutique Shippo Calculator

This version includes your uploaded Erendira's Boutique logo in `/public/eb-logo.png`.

The calculator is on the homepage, so the deployed link opens directly to the calculator.

## Setup

```bash
npm install
npm run dev
```

Create `.env.local` from `.env.example` and add your Shippo token plus origin address.
